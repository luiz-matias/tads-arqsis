import 'package:flutter/material.dart';

class LogicGatesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Layout here!"),
      ),
    );
  }
}
