import 'package:flutter/material.dart';

class AdderCircuitPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Layout here!"),
      ),
    );
  }
}
